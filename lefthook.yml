pre-commit:
    piped: true
    commands:
        4_phpcs:
            glob: '*.php'
            run: composer lint {staged_files}
        5_phpcbf:
            glob: '*.php'
            run: composer format {staged_files}
